% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_seurat_obj.R
\name{create_seurat_obj}
\alias{create_seurat_obj}
\title{Create Seurat Object with Strict Metadata Validation}
\usage{
create_seurat_obj(
  data_dir,
  metadata_file = NULL,
  species = NULL,
  enable_gene_sync = FALSE,
  min_cells = 3,
  min_features = 100,
  compress = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data_dir}{Character. Path to data directory}

\item{metadata_file}{Character. Path to metadata file (xlsx/xls). If NULL, will look for metadata.xlsx/xls in data_dir}

\item{species}{Character. Species for gene synchronization (e.g., "homo", "mus")}

\item{enable_gene_sync}{Logical. Enable gene name synchronization (default: FALSE)}

\item{min_cells}{Integer. Minimum cells per feature (default: 3)}

\item{min_features}{Integer. Minimum features per cell (default: 100)}

\item{compress}{Logical. Compress output (default: TRUE)}

\item{verbose}{Logical. Print detailed progress (default: TRUE)}
}
\value{
A Seurat object with integrated metadata
}
\description{
Main function to build Seurat objects with mandatory metadata validation.
Supports three data structures:
- Type A: Subfolder per sample
- Type B: Direct files in data_dir
- Type C: Single merged object
}
\details{
## Workflow
1. Validate metadata file (mandatory)
2. Detect data structure (Type A/B/C)
3. Match samples with metadata
4. Read and create Seurat objects
5. (Optional) Gene synchronization
6. Merge and integrate metadata
7. Return final object

## Metadata Requirements
- Column 1: samples (must match sample names)
- Column 2: groups (sample grouping)
- Additional columns: optional
}
